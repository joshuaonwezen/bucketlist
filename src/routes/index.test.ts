import '@testing-library/jest-dom'

import Index from './index.svelte';
import { render } from '@testing-library/svelte';


// test('Rules are shown', () => {
// 	const { getByText } = render(Index);

// 	expect(getByText('🍆 Rules 🍑')).toBeInTheDocument();
// });